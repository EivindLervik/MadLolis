using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmmunitionStorage : MonoBehaviour {

	private Dictionary<string, int> carry;

	private void Start(){
		/*
		carry = new Dictionary<string, int> ();
		foreach (AmmoType at in AmmoType) {
			carry.Add (at, 0);
		}
		*/
	}

}
